public static class Definition
{
    public const float ONE_BLOCK_DIST = 2.0f;
    public const float MAX_FIELD_X = ONE_BLOCK_DIST * 2;
    public const float MAX_FIELD_Y = ONE_BLOCK_DIST * 2;
    public const float MIN_FIELD_X = -1 * ONE_BLOCK_DIST * 2;
    public const float MIN_FIELD_Y = -1 * ONE_BLOCK_DIST * 2;
}